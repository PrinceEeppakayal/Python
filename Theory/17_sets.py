a = {1, 2, 3, 4, 5, 1}
print(type(a)) 
print(a) # It will remove the duplicate elements from the set

# NOTE: this syntax will create an empty dictionary and not an empty set
b = {}
print(type(b))

# NOTE: An empty set can be created using the below syntax:
c = set()
print(type(c))

