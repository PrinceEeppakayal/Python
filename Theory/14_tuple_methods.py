# Creating a tuple using ()
t = (1, 2 , 4, 5, 1, 1, 1)

# Count the number of times 1 appears in the tuple
print(t.count(1)) 

# Returns the index of the first occurrence of 5 in the tuple
print(t.index(5)) 
